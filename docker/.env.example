# Copy this file to the repository root as `.env` and fill in your
# Supabase project credentials (Supabase dashboard -> Project Settings ->
# API). The backend will not start serving data without them.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
