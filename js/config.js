export const API_BASE_URL = "http://localhost:5000/api";

/*
 * Google Maps Platform API key (Maps JavaScript API + Places + Geocoding).
 *
 * Create one at https://console.cloud.google.com/google/maps-apis and restrict
 * it to your site's HTTP referrers (e.g. http://127.0.0.1:5500/* and
 * http://localhost:5173/*). Leave empty to keep the current behavior:
 * "Detect" falls back to raw latitude/longitude from the browser.
 *
 * You can also inject the key at runtime without editing this file:
 *   <script>window.__FIXKART_GMAPS_KEY__ = "AIza..."</script>
 */
export const GOOGLE_MAPS_API_KEY = "";
